[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: pure unit tests without HTTP client
    api: FastAPI TestClient integration tests
