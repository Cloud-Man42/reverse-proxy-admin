# Managed by reverse-proxy-admin deploy/setup-modsecurity.sh
# Core ModSecurity settings (SecRuleEngine is set per proxy in waf-*.conf).
Include /etc/nginx/modsecurity/modsecurity-core.conf
Include /usr/share/modsecurity-crs/owasp-crs.load
